{
  "name": "x-saved-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^26.4.1",
    "esbuild": "^0.24.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
